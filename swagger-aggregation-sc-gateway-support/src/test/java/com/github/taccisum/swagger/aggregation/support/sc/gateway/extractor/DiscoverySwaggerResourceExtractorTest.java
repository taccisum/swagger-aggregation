package com.github.taccisum.swagger.aggregation.support.sc.gateway.extractor;

import org.junit.Test;
import org.springframework.cloud.gateway.route.RouteDefinition;
import springfox.documentation.swagger.web.SwaggerResource;

import static org.assertj.core.api.Assertions.assertThat;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;

public class DiscoverySwaggerResourceExtractorTest {
    @Test
    public void extract() {
//        RouteDefinition definition = mock(RouteDefinition.class);
//        when(definition.getPredicates())
//        SwaggerResource resource = new DiscoverySwaggerResourceExtractor().extract(definition);
//        assertThat(resource.getName()).isEqualTo("foo-service");
    }
}